[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA + customer links fall through to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
